/**
 * Structural markers for UI smoke / typecheck verification.
 * Routes: /, /employees, /attendance, /login
 * Tokens: canvas, m-stripe, hairline
 * Banner: RESEARCH DEMO
 */
export const UI_ROUTES = ["/", "/employees", "/attendance", "/login"] as const;
export const LICENSE_BANNER_TEXT =
  "RESEARCH DEMO — Face recognition models may be non-commercial";
